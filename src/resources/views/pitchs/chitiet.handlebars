{{!-- <div class="container mt-4">
    <div class="row">
        <div class="col-lg-3 d-flex align-items-center">
            <a href="#" class="btn btn-primary w-100">Đặt Sân</a>
        </div>
        <div class="col-lg-9">
            <h2>{{pitches.name}}</h2>
            <img class="card-img-top img-fluid" src="/img/{{pitches.image}}" alt="Card image cap">
            <p>{{pitches.describe}}</p>
        </div>
    </div>
</div> --}}

         <div class="container mx-auto p-6">
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <img src="/img/{{pitches.image}}" alt="Image" class="w-full h-64 object-cover rounded-t-lg">
            <div class="p-6">
                <h1 class="text-3xl font-bold mb-2">{{pitches.name}}</h1>
                <p class="text-gray-700 mb-6">Mô tả: {{pitches.describe}}</p>
                <p class=" text-red-600 mb-6">Giá: {{pitches.price}}</p>

                <button class="bg-blue-500 text-white px-4 py-2 rounded">Đặt sân</button>
            </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-lg mt-6">
            <h2 class="text-2xl font-bold mb-4">Other Products</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div class="bg-gray-100 p-4 rounded-lg shadow">
                    <img src="/img/{{pitches.image}}" alt="Product Image" class="w-full h-48 object-cover rounded">
                    <h3 class="text-lg font-semibold mt-2">{{pitches.name}}</h3>
                    <p class="text-gray-700">{{pitches.describe}}</p>
                    <p class="text-red-600 ">{{pitches.price}}</p>
                </div>
                <div class="bg-gray-100 p-4 rounded-lg shadow">
                    <img src="/img/{{pitches.image}}" alt="Product Image" class="w-full h-48 object-cover rounded">
                    <h3 class="text-lg font-semibold mt-2">{{pitches.name}}</h3>
                    <p class="text-gray-700">{{pitches.describe}}</p>
                    <p class="text-red-600 ">{{pitches.price}}</p>
                </div>
                <div class="bg-gray-100 p-4 rounded-lg shadow">
                    <img src="/img/{{pitches.image}}" alt="Product Image" class="w-full h-48 object-cover rounded">
                    <h3 class="text-lg font-semibold mt-2">{{pitches.name}}</h3>
                    <p class="text-gray-700">{{pitches.describe}}</p>
                    <p class="text-red-600 ">{{pitches.price}}</p>
                </div>
            </div>
        </div>
    </div>